<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_choose_photo"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="lmu.de.unificiencyandroid.components.settings.ChoosePhoto">
    <android.support.v7.widget.Toolbar
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/lmugreen"
        android:id="@+id/toolbar_choose_photo"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
        app:title="@string/camera_album"/>

    <LinearLayout
        android:layout_below="@id/toolbar_choose_photo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="30dp">
        <Button
            android:padding="20dp"
            android:layout_marginLeft="30dp"
            android:id="@+id/camera"
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:backgroundTint="@color/lmugreen"
            android:background="@drawable/camera" />
        <Button
            android:padding="20dp"
            android:layout_marginLeft="10dp"
            android:id="@+id/album"
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:backgroundTint="@color/lmugreen"
            android:background="@drawable/album" />
    </LinearLayout>

</RelativeLayout>
